# Frontend environment variables
# Copy this file to .env.local and adjust as needed

# Backend API URL — change this when deploying to a remote server
# Default (localhost dev): leave commented out or set to http://localhost:8000
# VITE_API_BASE_URL=http://localhost:8000
